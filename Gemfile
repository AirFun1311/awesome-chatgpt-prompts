source "https://rubygems.org"

# Use GitHub Pages meta-gem which pins compatible Jekyll/plugins
# Do not add a separate `jekyll` gem when using github-pages

gem "github-pages", group: :jekyll_plugins

group :development, :test do
  gem "rake", "~> 13.2"
  gem "html-proofer", "~> 5.0"
end 